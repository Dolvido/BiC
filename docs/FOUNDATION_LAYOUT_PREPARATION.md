# Layout comparison preparation record

The first data preparation attempt failed before constructing a learner or
performing a GPU operation. The original-layout admission and canonical
evidence passes completed. During the paired layout pass, an exact observed
transcript collided with the protected inventory or a different admitted pair.
The guard rejected it after 497 canonical bundles and 994 layout bundles had
been materialized. This is a data-admission failure, not a learning result.

The enclosing call cost 703.907 wall seconds and 691.65625 process CPU seconds.
It returned 1,258,868 canonical episode copies across initial generation and
reconstruction; that is not a count of distinct training lessons. All seven
neural/optimizer/CUDA guards recorded zero attempts. The failed receipts and
74 exact source snapshots are preserved under
`runs/foundation-layout-preparation-local/attempt-001`.

The amendment admits both layout views of a complete pair together. If either
view collides, it changes only the pair's naming realization, using the existing
deterministic bounded naming-attempt namespace. Both arms receive that same
renamed canonical pair. Problems, values, answers, read-version ancestry, layout
seed, query-placement policy and planned exposures must remain unchanged.
Rejected candidates do not enter the accepted transcript inventory. No
performance score is available or used for this change.

Preparation must record each conflict's location, transcript digest and
collision category, all rejected work, final naming overrides and the prior
failed attempt's cost. It must verify canonical evidence against the repaired
plan and perform all existing truth/ancestry checks. A new guarded attempt can
start only after the amended source is reviewed; the failed attempt is never
resumed or overwritten. No new preparation or training outcome is recorded by
this note.
