# Explicit recovery: count valid repeated practice accurately

The first continuous campaign stopped before any learner update. Its accepted
local tutor recipe compiled into 6,912 distinct non-replay transcripts, 48 of
which matched admitted original training examples. The added fresh-only check
rejected those valid repeated examples after the tutor compiler had finished.
No protected evaluation material was admitted and no learner checkpoint changed.

The failed attempt and all original sources remain immutable. It spent
130.156 seconds in the owner, including 118.422 seconds in compilation, one
accepted teacher request, and zero learner updates. Both procedural and tutor
compiler calls completed; only the 108 procedural archives were published.
The initial 34.750-second CPU metadata/protection freeze is a separate recorded
cost. The small prior diagnostic finding zero matches in 288 old rows did not
establish absence of matches in this new complete curriculum.

The additive v2 data adapter treats original admitted training overlap as
repeated practice. It reports novel and repeated non-replay transcripts and
retains their exact identities. This changes neither the saved teacher recipe
nor the canonical compiler, truth checks, historical exclusions, held-out
exclusions, nor paired exposure and evaluation rules. Literal rehearsal remains
valid. Future exclusion lists omit these original-training overlaps so that
canonical rehearsal stays admitted. All repeated work remains visible.

The additive v2 owner admits only the authenticated first-cycle authored-stage
failure, with a completed accepted teacher request, no GPU child, zero learner
updates and no unknown work. It reuses that exact author decision and context;
it does not contact the teacher again for this cycle. The adapter authenticates
and copies the successfully persisted procedural archives without generating
them again, then materializes the tutor and common-withdrawal streams. Both old
completed compiler calls remain charged; regeneration of the unpublished tutor
stream is new work.

The recovery receives floor(3,600 - 130.156) = 3,469 worker seconds. New metadata
freeze work is recorded separately. The previous invocation's frozen budget is
not silently reset. Its full failed receipt, staged state, author decision,
context and procedural output are pinned by the recovery launch.

All other mechanics remain those of [the original campaign](CONTINUOUS_TUTOR_CAMPAIGN.md):
matched native branches, full optimizer restarts, teacher-free withdrawal,
fixed compute admission and watchdog, raw score records, independent recounts,
and the retention guard against both current and campaign-start abilities.
The unchanged worker admits the v2 producer's actual source pins; no checkpoint
origin or model recipe is relabeled. Later v2 cycles prepare their own contexts
and request new compact recipes normally.

This is a correction to an unrequested novelty restriction, not evidence of a
tutor learning advantage. Real recurring-cycle completion and native benefit
remain to be measured. These lessons still use the declared restricted English
grammar; ordinary explanations and new meanings remain future capability work.
