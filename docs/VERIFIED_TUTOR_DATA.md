# Verified tutor data boundary

Two separate, exclusive CPU preparations precede learning. Neither constructs a
model, performs a backward pass, initializes CUDA or contacts a tutor. A failed
attempt preserves its completed artifacts and receipt and has no automatic retry.

Inventory authenticates the original admitted cycle-zero plan and 216 cached
bundles, without reconstructing canonical lessons. Its 3,456 all-family parent
descriptors bind original plan, canonical bundle, pair index and three canonical
pair hashes. Parent pool features count copy operations in the final anchor's
exact version ancestry, equally across all three domains. Each of 18 depth/length
cells supplies 16 balanced quantiles, 16 greatest-copy parents and 16 least-copy
parents, with canonical IDs breaking ties. When all copy counts tie, all pools
use the same balanced descriptors. Record actual pool counts, ranges and overlaps;
pool names describe relative emphasis, not new skills or semantic operators.
Replay uses the complete original parent inventory in fixed cache order.

Protect the authenticated cached historical union (1,635,840 transcripts), all
2,952 original reserved transcripts and four subsequent 720-row development banks:
exactly 1,641,672 distinct transcripts. Verify disjointness from 62,208 admitted
training transcripts; those training transcripts remain allowed replay. Historical
inventory reads the preserved completed history phase and does not regenerate it.
Copy only current continuation development720, actual training-fit108 and retention720
as unchanged evaluation banks. They have already been observed in development.

Teaching has six chapters, each with all depth0..5 by length8/10/12 cells in that
order: 108 complete three-family updates at microbatch32, starting cursor1944.
Chapters0 and1 are fixed replay; chapters2..5 allow all three pools and independent,
rename or revalue realizations. The default is balanced/independent. Both arms share
seed852802001 and ordered slots. Common withdrawal starts2052 and uses108 fixed
replay updates with no author input. Compiler provenance preserves canonical
coordinates separately from consumed global IDs.

Compilation reads and authenticates the original persisted author result, intent
and response. Its actual parent identity, weights, lifetime1944, contract and recipe
pins must match. A caller dictionary cannot substitute a different recipe. Save
the compiler manifests and provenance as well as every immutable bundle image.
Only one common withdrawal stream is serialized and both arms reference it.

Every stream has the same cap of10,368 episodes,10,616,832 observation bytes and
3,981,312 reply-target bytes. With at most124,416 turns, derived token caps are
10,865,664 observation tokens including BOS/EOS and4,105,728 reply tokens including
EOS. These are equal upper limits, not a claim of equal actual token/byte cost.
Each compiler is bounded by250,000 canonical generation calls and600 seconds;
the encompassing compilation has1,800 seconds including verification/publication.
Inventory has600 seconds. Deadlines are checked at boundaries, not preemptive.

Each stage preserves source/input/artifact hashes, archive attempts/completions,
bytes decoded, actual generator and validation work, costs and terminal failures.
Nested compiler/archive timing is already included in the outer wall/CPU cost.
Inventory requires223 archive loads: four bank archives, training/reserved/history
and216 cached bundles. Compilation decodes only the protected transcript archive;
evaluation bank bytes are copied without decoding. No training archive is rewritten.
Treatment reports actual differing teaching row digests and actual exposures.
Identical materialized curricula mean zero contrast regardless of recipe wording.
