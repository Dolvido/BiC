# Complementary composition acquisition and transfer

This prospective local comparison tests the curriculum in
`COMPLEMENTARY_COMPOSITION_DESIGN.md`. It does not change the learner model,
full AdamW state, learning rate, objective weights or strict runtime. Both arms
start from the exact update-9,760 research checkpoint with SHA256
`5548a2f46897f33f32afd5320b348756fc2c591a81cd27ba009e8d1ddd8a39f8`.
The retained update-9,112 campaign reference remains separate. No automatic
adoption or retry is authorized by this experiment.

Each arm receives 648 updates, with equal color/count/switch microbatches of
32 episodes per update. In 108 groups the order is learning, broad rehearsal,
prior definition, broad rehearsal, learning, broad rehearsal. Control learning
slots cycle twice through the 108 original basis images. For the curriculum
arm, every sixth learning slot is atomic: cycle through the six chapter cells
six times, taking the first six images in each. Its other 180 learning slots
cycle through ten complementary contrast/mode cells, with eighteen images per
cell. Both arms receive the same 108 prior-definition images and 324 broad
rehearsal images in the same positions. These are repeated, admitted lessons.

Complementary meanings are CD/DC, SD/DD, SS/DS, DCD/CDD and SSD/SDD, each in
binding and revision contexts. The exact SC/CS/SCS/CSS programs remain absent
from new training. Every pair is checked by independent typed and English
interpreters, with prefix-state targets derived from visible history. Preserve
split-disjoint nonce vocabularies, exact train/development text separation,
original protected banks, context limits and opposite known answer anchors.
Training novelty means new admitted content, not a prohibition on useful
repetition. Report repeated rows honestly. Structural holdout does not imply
new functions in every finite family: switch equivalences remain disclosed.

Before training, save immutable data and source hashes. Use one inclusive
1,200-second worker allowance for both arms and all preparation, restoration,
evaluation and publication within the worker. Separate CPU data preparation
and freeze costs are reported. Run control then curriculum from the same full
parent. No teacher call occurs in data preparation or learning; this experiment
does not attribute curriculum authorship or success to the earlier LLM order.

Evaluate steps 0, 216, 432 and 648. Save and exactly restore full state before
each trained endpoint. Evaluate all twelve original banks unchanged, plus
complementary development binding/revision and exact training-fit binding/
revision. Development has eight pairs per contrast/family, 120 pairs per panel.
Training fit has the first four pairs per family from the first admitted image
of each contrast/mode cell, sixty pairs per panel. Fit and development are
different measurements, with native actions and freely generated replies in
both. Four endpoints across two arms means eight restorations and six snapshots;
physical training is 1,296 updates and 124,416 episode exposures if complete.

The primary contrast is final all-query complete-pair accuracy on the unchanged
basis composition and sequence panels, averaged equally across six family
cells. Curriculum benefit requires at least five percentage points over control
and no negative difference in any of those cells. Report both intermediate
acquisition panels and all earlier endpoints without replacing this metric.
The complementary acquisition milestone requires at least 75% complete pairs
in every binding/revision family cell of both fit and fresh-name development.
The separate original basis capability milestone still requires 75% in each of
twelve binding/revision/composition/sequence family cells.

For candidate eligibility, require positive final composition/sequence gain
over the current parent and at most five percentage points lost in every
original retention check against both the current research parent and retained
reference: 75 broad bank/family joint-pair and known/unknown action/reply checks,
nine prior-definition complete-pair checks and twelve basis complete-pair
checks. Neither a control win nor a small development lead authorizes adoption.
There is no separate withdrawal phase: broad rehearsal is interleaved and the
whole run is independent of tutor requests. Do not claim withdrawal evidence
from this schedule.

Independently recount saved raw answers, complete pair inventories, actual
source recipes, consumed lessons, state restoration and physical costs. Preserve
failures and unknown work without inventing completed updates. Count actual
text tokens and compute; matched updates do not imply matched byte cost. These
are repeatedly observed development banks and one continuing learner, not a
fresh audit, general English mastery or learned self-direction.
