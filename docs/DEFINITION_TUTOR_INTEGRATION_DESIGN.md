# Give the external tutor a shared skill progression

The current author selects earlier parent pools and alias/value transformations.
It cannot choose reusable definition skills. The recurring campaign exposed
both finite fresh-example exhaustion and recipes identical to the procedural
default. Neither outcome establishes teacher benefit.

After measuring the fixed basis experiment, add a versioned chapter adapter at
the existing author-to-compiler boundary. Let the external tutor choose a
compact progression among copy, advance-source, advance-destination, revision
and selecting between two named rules. Apply that progression across all three
families; preserve equal family allocation, exposure limits and broad rehearsal.

The independent provider owns valid English, causal execution, counterfactual
answers and state labels. It must admit each generated pair and supply prefix
labels through the existing public learner interface. The teacher cannot change
correctness, protected evaluation words or withheld composition/sequence
programs. Teacher commentary receives no credit as an instruction unless the
learner actually receives it.

Admitted training practice can repeat. Report repetition without requiring
unbounded novelty. A proposal that cannot be compiled must stop or use explicitly
reported replay/fallback. Before running paired trajectories, compare the entire
ordered training input and target stream plus full starting state. Identical
branches should execute once, with the reused result and actual physical work
declared as zero teaching contrast. This is a future optimization; the current
frozen campaign continues its original two-branch protocol.

Compare useful tutor progression against a fixed procedural progression from
the same full learner and optimizer. Follow both with common tutor-free
withdrawal. Measure all-query paired native actions and free replies on new
bindings and withheld combinations, together with old-skill retention. A later
cycle with zero tutor requests must preserve measured gains before claiming
reduced dependence. Retention decisions remain explicit and reversible.

This is proposed integration work. The existing basis experiment uses verified
procedural lessons and establishes neither a tutor advantage nor learned
self-direction by itself.
